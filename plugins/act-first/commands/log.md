---
description: Record what a run actually taught — the gap between prediction and result
---

Apply the `learning-log` skill to: $ARGUMENTS

Append to `log/YYYY-MM.md` using the four-line format. Lead with the gap between
what was predicted and what happened, and add a `Reusable` line if the run
produced a rule of thumb worth carrying to a different problem.

If $ARGUMENTS is a question about what's been learned rather than something to
record, grep the log and answer from it, leading with the `Reusable` lines.
