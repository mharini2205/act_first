---
description: Something won't run — diagnose fast, then downgrade to a surface that still teaches
---

Apply the `unstick` skill to: $ARGUMENTS

Run the fast diagnostic checks first — especially the vendor's unmodified
example, which splits "my input is wrong" from "my install is broken."

If the blocker isn't resolved quickly, propose a specific rung on the downgrade
ladder and state what it preserves and what it loses. Don't leave the user
fighting an installer with nothing to learn from.
